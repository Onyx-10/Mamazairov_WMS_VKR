// frontend/src/pages/LoginPage.tsx
import React from 'react'

const LoginPage: React.FC = () => {
  return (
    <div>
      <h1>Login Page</h1>
      <p>Login form will be here.</p>
      {/* 
        Убери любой другой код, который мог быть здесь, особенно на строке 4
        после этого объявления компонента, если только это не 'export default LoginPage;'
      */}
    </div>
  );
};

export default LoginPage;